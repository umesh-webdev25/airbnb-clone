<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Final Page</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-pink-100 via-white to-pink-50 min-h-screen flex flex-col">

  <!-- Nav Bar at Top -->
  <%- include('../partials/nav') %>

  <!-- Page Content -->
  <div class="flex-grow flex items-center justify-center">
    <!-- Success Card -->
    <div class="bg-white p-10 rounded-2xl shadow-lg text-center max-w-md w-full border border-pink-200 animate-fadeIn">
      <!-- Icon -->
      <div class="flex justify-center mb-4">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 text-green-500" fill="none" viewBox="0 0 24 24"
          stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
        </svg>
      </div>

      <!-- Heading -->
      <header class="mb-4">
        <h1 class="text-3xl font-bold text-pink-600">Registration Successful 🎉</h1>
      </header>

      <!-- Message -->
      <p class="text-gray-600 mb-6">
        Your account has been created successfully. You can now explore our platform.
      </p>

      <!-- Button -->
      <a href="/" class="inline-block bg-pink-500 hover:bg-pink-600 text-white font-semibold px-6 py-3 rounded-full shadow-md transition-all duration-300">
        Go to Home
      </a>
    </div>
  </div>

  <!-- Animation -->
  <style>
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    .animate-fadeIn {
      animation: fadeIn 0.6s ease-out;
    }
  </style>
  <footer class="bg-white border-t border-gray-200 py-8">
    <div class="container mx-auto px-4">
      <p class="text-center text-gray-500 text-sm">
        &copy; 2023 Dream Homes. All rights reserved.
      </p>
    </div>
  </footer>
</body>
</html>
