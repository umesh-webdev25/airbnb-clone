<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Airbnb App | User Management</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: '#667eea',
            secondary: '#764ba2',
            airbnb: {
              dark: '#222222',
              light: '#717171',
            }
          },
          boxShadow: {
            'airbnb': '0 6px 16px rgba(0, 0, 0, 0.12)',
            'airbnb-hover': '0 8px 24px rgba(0, 0, 0, 0.16)'
          }
        }
      }
    }
  </script>
  <style>
    .user-card {
      transition: all 0.3s ease;
      background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
    }

    .user-card:hover {
      transform: translateY(-8px) scale(1.02);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    }

    .avatar-container {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }

    .role-badge {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
    }

    .status-indicator {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      box-shadow: 0 2px 8px rgba(16, 185, 129, 0.3);
    }
  </style>
</head>

<body class="min-h-screen bg-gray-50">

  <!-- Navbar -->
  <%- include('../partials/nav') %>

  <!-- Main Content -->
  <main class="min-h-screen bg-gradient-to-br from-gray-50 via-white to-purple-50">
    <div class="container mx-auto px-4 py-8 max-w-7xl">
      <!-- Header Section -->
      <div class="bg-white rounded-2xl shadow-lg border border-gray-100 overflow-hidden mb-8">
        <div class="px-8 py-8 border-b border-gray-100">
          <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between">
            <div class="mb-6 lg:mb-0">
              <h1 class="text-4xl font-bold text-airbnb-dark mb-2">
                <i class="fas fa-users text-primary mr-3"></i>
                User Management Dashboard
              </h1>
              <p class="text-airbnb-light text-lg">Manage and oversee all registered users in your system</p>
              <div class="flex items-center mt-4 space-x-4">
                <div class="flex items-center text-sm text-gray-600">
                  <i class="fas fa-user-check text-green-500 mr-2"></i>
                  <span class="font-medium"><%= Registration.length %></span> Active Users
                </div>
                <div class="flex items-center text-sm text-gray-600">
                  <i class="fas fa-calendar-alt text-primary mr-2"></i>
                  Last updated: <%= new Date().toLocaleDateString() %>
                </div>
              </div>
            </div>
            <div class="flex flex-col sm:flex-row gap-3">
              <a href="/host/home"
                 class="inline-flex items-center px-6 py-3 bg-gradient-to-r from-primary to-secondary text-white rounded-xl hover:from-secondary hover:to-primary transition-all shadow-lg hover:shadow-xl transform hover:-translate-y-0.5">
                <i class="fas fa-plus mr-3"></i>
                Add New User
              </a>
            </div>
          </div>
        </div>
      </div>

      <!-- Users Grid -->
      <div class="bg-white rounded-2xl shadow-lg border border-gray-100 overflow-hidden">
        <div class="p-8">
          <% /* If a single `user` is passed to this view, show the user's detail + their listings.
                 Otherwise show the users grid from `Registration` (existing behavior). */ %>
          <% if (typeof user !== 'undefined' && user) { %>
            <div class="max-w-6xl mx-auto">
              <div class="flex flex-col lg:flex-row gap-8">
                <!-- Left: Sticky User Card -->
                <aside class="w-full lg:w-1/3 sticky top-24 self-start">
                  <div class="bg-white rounded-2xl border border-gray-100 shadow-airbnb p-6">
                    <div class="overflow-hidden rounded-xl mb-4">
                      <% // normalize stored image path: allow 'uploads/..' or '/uploads/...'
                         const profileSrc = (user && user.profileImage)
                           ? (user.profileImage.startsWith('/') ? user.profileImage : ('/' + user.profileImage))
                           : ('https://ui-avatars.com/api/?name=' + encodeURIComponent(user.name) + '&background=ffffff&color=667eea&size=600');
                      %>
                      <img src="<%= profileSrc %>" alt="<%= user.name %>" class="w-full h-56 object-cover" loading="lazy" onerror="this.onerror=null;this.src='https://via.placeholder.com/900x600?text=No+Image'">
                    </div>

                    <h2 class="text-2xl font-bold text-airbnb-dark mb-1"><%= user.name %></h2>
                    <p class="text-sm text-gray-600 break-all mb-3">Email : <%= user.email %></p>

                    <div class="text-sm text-gray-500 space-y-2">
                      <div>Username: <span class="font-medium text-gray-700"><%= user.username %></span></div>
                      <div>Role: <span class="capitalize font-medium"><%= user.role %></span></div>
                      <div>Joined: <span class="text-gray-400"><%= new Date(user.createdAt).toLocaleDateString() %></span></div>
                      <div>Listings: <span class="font-medium text-gray-700"><%= user.listings ? user.listings.length : 0 %></span></div>
                    </div>

                    <div class="mt-4 flex gap-2">
                      <a href="/host/editing/<%= user._id %>?editing=true" class="flex-1 px-3 py-2 bg-gradient-to-r from-primary to-secondary text-white rounded-lg text-sm text-center">Edit</a>
                      <form action="/host/delete/<%= user._id %>" method="POST" class="flex-1">
                        <button type="submit" class="w-full px-3 py-2 bg-red-500 text-white rounded-lg text-sm">Delete</button>
                      </form>
                    </div>
                  </div>
                </aside>

                <!-- Right: Listings by user -->
                <section class="w-full lg:w-2/3">
                  <div class="mb-4">
                    <h3 class="text-lg font-semibold">Listings by <%= user.name %></h3>
                    <p class="text-sm text-gray-500">All properties created by this user</p>
                  </div>

                  <% if (user.listings && user.listings.length > 0) { %>
                    <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
                      <% user.listings.forEach(listing => { %>
                        <article class="bg-white border border-gray-100 rounded-lg overflow-hidden hover:shadow-lg transition">
                          <a href="/listings/<%= listing._id %>" class="block">
                            <div class="h-40 w-full overflow-hidden bg-gray-100">
                <% const listingSrc = listing.imageUrl
                  ? (listing.imageUrl.startsWith('/') ? listing.imageUrl : ('/' + listing.imageUrl))
                  : 'https://images.unsplash.com/photo-1564013799919-ab600027ffc6?auto=format&fit=crop&w=1200&q=60'; %>
                <img src="<%= listingSrc %>" alt="<%= listing.home %>" class="w-full h-full object-cover" loading="lazy" onerror="this.onerror=null;this.src='https://via.placeholder.com/1200x800?text=No+Image'">
                            </div>
                            <div class="p-4">
                              <div class="flex items-start justify-between">
                                <div>
                                  <h4 class="font-semibold text-lg"><%= listing.home %></h4>
                                  <p class="text-sm text-gray-500">₹<%= listing.price.toLocaleString() %> • <%= listing.city %>, <%= listing.country %></p>
                                </div>
                                <div class="text-sm text-gray-500"><%= listing.bedrooms ? listing.bedrooms + 'bd' : '' %></div>
                              </div>
                              <p class="text-xs text-gray-400 mt-3"><%= listing.description ? (listing.description.length > 120 ? listing.description.substring(0, 120) + '...' : listing.description) : 'No description available' %></p>
                              <div class="mt-3 flex items-center justify-between text-xs text-gray-500">
                                <span>Updated: <%= new Date(listing.updatedAt || listing.createdAt).toLocaleDateString() %></span>
                                <a href="/listings/<%= listing._id %>" class="text-primary font-medium">View details</a>
                              </div>
                            </div>
                          </a>
                        </article>
                      <% }) %>
                    </div>
                  <% } else { %>
                    <div class="text-center py-12 text-gray-600">
                      <p>No listings found for this user.</p>
                      <a href="/host/home" class="inline-block mt-4 px-4 py-2 bg-gradient-to-r from-primary to-secondary text-white rounded-lg">Add Listing</a>
                    </div>
                  <% } %>
                </section>
              </div>
            </div>

          <% } else { %>

            <% if (Registration.length > 0) { %>
              <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
                <% Registration.forEach(user => { %>
                  <div class="bg-white rounded-2xl border border-gray-100 shadow-airbnb p-4 flex flex-col h-full">
                    <div class="overflow-hidden rounded-lg h-40 w-full">
                      <% const gridSrc = user.profileImage
                          ? (user.profileImage.startsWith('/') ? user.profileImage : ('/' + user.profileImage))
                          : ('https://ui-avatars.com/api/?name=' + encodeURIComponent(user.name) + '&background=ffffff&color=667eea&size=300'); %>
                      <img src="<%= gridSrc %>" alt="<%= user.name %>" class="w-full h-full object-cover" loading="lazy" onerror="this.onerror=null;this.src='https://via.placeholder.com/600x400?text=No+Image'">
                    </div>

                    <div class="mt-4 flex-1">
                      <h3 class="font-semibold text-lg truncate"><%= user.name %></h3>
                      <p class="text-xs text-gray-500 break-all"><%= user.email %></p>
                      <p class="text-xs text-gray-500 mt-2">Listings: <span class="font-medium text-gray-700"><%= user.listings ? user.listings.length : 0 %></span></p>
                    </div>

                    <div class="mt-4 flex gap-2">
                      <a href="/host/home/<%= user._id %>" class="flex-1 px-3 py-2 bg-gradient-to-r from-primary to-secondary text-white rounded-lg text-sm text-center">Details</a>
                      <a href="/host/user-listings/<%= user._id %>" class="flex-1 px-3 py-2 border border-gray-200 rounded-lg text-sm text-center">View listings</a>
                    </div>
                  </div>
                <% }) %>
              </div>
            <% } else { %>

              <!-- Enhanced Empty State -->
              <div class="text-center py-20">
                <div class="mx-auto w-32 h-32 bg-gradient-to-br from-primary to-secondary rounded-full flex items-center justify-center mb-6 text-white shadow-2xl">
                  <i class="fas fa-users text-5xl"></i>
                </div>
                <h3 class="text-2xl font-bold text-gray-800 mb-2">No Users Found</h3>
                <p class="text-gray-500 text-lg mb-8 max-w-md mx-auto">Get started by adding your first user to the system. Users will appear here once registered.</p>
                <a href="/host/home"
                   class="inline-flex items-center px-8 py-4 bg-gradient-to-r from-primary to-secondary text-white rounded-xl hover:from-secondary hover:to-primary transition-all shadow-lg hover:shadow-xl transform hover:-translate-y-1 text-lg font-medium">
                  <i class="fas fa-plus mr-3"></i>
                  Add Your First User
                </a>
              </div>
            <% } %>

          <% } %>
        </div>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer class="w-full bg-gradient-to-r from-gray-800 to-gray-900 mt-16 py-8">
    <div class="container mx-auto px-4 max-w-7xl">
      <div class="flex flex-col md:flex-row md:items-center md:justify-between">
        <div class="mb-4 md:mb-0">
          <p class="text-gray-300 text-sm">
            © 2023 Airbnb Clone. All rights reserved.
          </p>
        </div>
        <div class="flex items-center space-x-6">
          <div class="flex items-center text-gray-400 text-sm">
            <i class="fas fa-shield-alt mr-2 text-primary"></i>
            Secure & Private
          </div>
          <div class="flex items-center text-gray-400 text-sm">
            <i class="fas fa-clock mr-2 text-green-400"></i>
            24/7 Support
          </div>
        </div>
      </div>
    </div>
  </footer>

</body>
</html>